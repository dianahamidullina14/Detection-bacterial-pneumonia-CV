# Detection-bacterial-pneumonia-CV
The project develops a CNN model for automatic detection of bacterial pneumonia from chest X-rays. It implements binary classification, data preprocessing, GPU training, and evaluation of accuracy, precision, and F1 score.
